{
  "name": "subtovoice",
  "version": "0.1.0",
  "private": true,
  "description": "Natural buffered TTS for YouTube subtitles",
  "scripts": {
    "test": "node --test tests/*.test.js",
    "check": "node --check src/subtitle-buffer.js && node --check src/speech-controller.js && node --check src/content.js && node --check popup.js"
  }
}
